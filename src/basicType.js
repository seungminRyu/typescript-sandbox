var count = 0;
count += 1;
// count = '갑자기 문자열은 안됑 ㅎ';
var message = 'hello world';
var done = true;
var numbers = [1, 2, 3];
var messages = ['hello', 'world'];
// messages.push(1); // 숫자 push 하면 안된다.
var mightBeUndefined = undefined; // string 일수도 있고 undefined 일수도 있고
var nullableNumber = null; // number 일 수도 있고 null일 수 도 있고
var color = 'red'; // red, orange, yellow 중 한개다.
color = 'yellow';
// color = 'green'; // 에러 발생
